apiVersion: v1
kind: Pod
metadata:
  name: ubuntu-noble-claude
  labels:
    app: ubuntu-noble-claude
spec:
  containers:
  - name: ubuntu-noble-claude
    image: sciencedata/ubuntu_noble_claude_sciencedata
    volumeMounts:
      - name: sciencedata
        mountPath: "/home/claude/code"
    ports:
    - containerPort: 22
      protocol: TCP
    env:
    - name: SSH_PUBLIC_KEY
      value: ""
    - name: USERNAME
      value: "claude"
    - name: HOME_SERVER
      value: ""
    - name: SD_UID
      value: ""
    - name: PUBLIC_HOME_SERVER
      value: ""
    securityContext:
      capabilities:
        add:
          - "SYS_CHROOT"
          - "AUDIT_WRITE"
          - "NET_ADMIN"
  restartPolicy: Never
