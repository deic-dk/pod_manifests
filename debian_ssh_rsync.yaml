apiVersion: v1
kind: Pod
metadata:
  name: debian_ssh_rsync
  labels:
    app: debian_ssh_rsync
spec:
  containers:
  - name: debian_ssh_rsync
    image: codeberg.org/codeberg-infrastructure/backup
    volumeMounts:
      - name: sciencedata
        mountPath: "/data"
    ports:
    - containerPort: 22
      protocol: TCP
    env:
    - name: SSH_PUBLIC_KEY
      value: ""
    - name: USERNAME
      value: "data"
    - name: HOME_SERVER
      value: ""
    - name: SD_UID
      value: ""
    - name: PUBLIC_HOME_SERVER
      value: ""
    securityContext:
      capabilities:
        add:
          - "SYS_CHROOT"
          - "AUDIT_WRITE"
          - "NET_ADMIN"
  restartPolicy: Never
